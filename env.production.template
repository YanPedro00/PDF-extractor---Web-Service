# Variáveis de Ambiente para Produção - Oracle OCI
# Copie este arquivo para .env na instância OCI e configure os valores

# URLs das APIs (Railway por enquanto, depois mover para OCI)
NEXT_PUBLIC_API_URL=https://pdf-ocr-api-production.up.railway.app
NEXT_PUBLIC_TIFF_API_URL=https://tiff-to-pdf-api-production.up.railway.app

# URL do site (substitua pelo seu IP público ou domínio)
NEXT_PUBLIC_SITE_URL=http://SEU_IP_PUBLICO_AQUI

# Node
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Webhook Secret (para deploy automático via GitHub)
# Gere com: openssl rand -hex 32
WEBHOOK_SECRET=change-me-to-a-secure-secret

