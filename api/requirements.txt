# API Flask
flask==3.1.2
flask-cors==6.0.2
flask-limiter==3.5.0
gunicorn>=21.2.0

# Processamento de dados e Excel
pandas>=2.1.4
openpyxl>=3.1.2

# Processamento de imagens (requerido por img2table)
Pillow>=10.0.0

# PDF e OCR
PyMuPDF>=1.23.0
img2table>=1.4.2

# NOTA IMPORTANTE:
# PaddlePaddle e PaddleOCR são instalados com VERSÕES FIXAS no Dockerfile
# para garantir compatibilidade ARM64:
# - paddlepaddle==2.6.0 (última versão estável ARM64)
# - paddleocr==2.7.0.3 (compatível com 2.6.0)
# 
# NÃO adicionar aqui para evitar conflitos de versão!
